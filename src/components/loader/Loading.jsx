import Loader from '../../assets/loaderSvg/Loader.svg';

const Loading = () => {
  return (
    <div className=' flex items-center justify-center'>
      <img src={Loader} alt='' />
    </div>
  );
};

export default Loading;
